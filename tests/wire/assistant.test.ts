// This file was auto-generated by Fern from our API Definition.

import { VideoGenClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("AssistantClient", () => {
    test("startAssistantChat", async () => {
        const server = mockServerPool.createServer();
        const client = new VideoGenClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { message: "A 30-second explainer about our new pricing tiers" };
        const rawResponseBody = {
            projectId: "projectId",
            projectUrl: "projectUrl",
            reply: "reply",
            workflowSuggestions: [
                { actionId: "actionId", workflowType: "workflowType", title: "title", description: "description" },
            ],
            actions: [{ actionId: "actionId", kind: "APPROVE_PLAN", label: "label", requiresApp: true }],
            status: "idle",
            generation: {
                workflowRunId: "workflowRunId",
                projectId: "projectId",
                projectUrl: "projectUrl",
                remixActionIds: ["remixActionIds"],
            },
        };

        server
            .mockEndpoint()
            .post("/v1/assistant/chats")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.assistant.startAssistantChat({
            message: "A 30-second explainer about our new pricing tiers",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("sendAssistantMessage", async () => {
        const server = mockServerPool.createServer();
        const client = new VideoGenClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = { message: "Make it more upbeat and add captions" };
        const rawResponseBody = {
            projectId: "projectId",
            projectUrl: "projectUrl",
            reply: "reply",
            workflowSuggestions: [
                { actionId: "actionId", workflowType: "workflowType", title: "title", description: "description" },
            ],
            actions: [{ actionId: "actionId", kind: "APPROVE_PLAN", label: "label", requiresApp: true }],
            status: "idle",
            generation: {
                workflowRunId: "workflowRunId",
                projectId: "projectId",
                projectUrl: "projectUrl",
                remixActionIds: ["remixActionIds"],
            },
        };

        server
            .mockEndpoint()
            .post("/v1/assistant/chats/projectId/messages")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.assistant.sendAssistantMessage({
            projectId: "projectId",
            message: "Make it more upbeat and add captions",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("actOnAssistantAction", async () => {
        const server = mockServerPool.createServer();
        const client = new VideoGenClient({ maxRetries: 0, token: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            projectId: "projectId",
            projectUrl: "projectUrl",
            reply: "reply",
            workflowSuggestions: [
                { actionId: "actionId", workflowType: "workflowType", title: "title", description: "description" },
            ],
            actions: [{ actionId: "actionId", kind: "APPROVE_PLAN", label: "label", requiresApp: true }],
            status: "idle",
            generation: {
                workflowRunId: "workflowRunId",
                projectId: "projectId",
                projectUrl: "projectUrl",
                remixActionIds: ["remixActionIds"],
            },
        };

        server
            .mockEndpoint()
            .post("/v1/assistant/chats/projectId/actions/assistantActionId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.assistant.actOnAssistantAction({
            projectId: "projectId",
            assistantActionId: "assistantActionId",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
