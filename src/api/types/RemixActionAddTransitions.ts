// This file was auto-generated by Fern from our API Definition.

import type * as VideoGenApi from "../index.js";

/**
 * Stamp transitions across the project. Not per-boundary: each field you set is applied uniformly to every boundary in that scope, replacing any transition already there. Set the transition between sections, between base-layer assets, or both; omit or pass `null` for a scope to leave its transitions untouched. To change transitions on specific scenes only, use `EDIT_WITH_AGENT` instead.
 */
export interface RemixActionAddTransitions {
    /** Transition applied at every boundary between sections, replacing any existing section transitions. Omit or pass `null` to leave section transitions untouched. */
    sectionTransition?: (VideoGenApi.RemixTransitionStyle | null) | undefined;
    /** Transition applied at every boundary between base-layer assets within sections, replacing any existing asset transitions. Omit or pass `null` to leave asset transitions untouched. */
    assetTransition?: (VideoGenApi.RemixTransitionStyle | null) | undefined;
}
