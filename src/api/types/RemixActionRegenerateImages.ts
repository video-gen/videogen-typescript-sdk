// This file was auto-generated by Fern from our API Definition.

/**
 * Restyle every eligible still image in the project to a new look (image-to-image), replacing each image in place. Eligible images are non-SVG image assets backed by an uploaded or generated file. Runs asynchronously: one restyled image is generated per eligible image. If the project has no eligible images, the action is skipped and completes successfully without changing anything.
 */
export interface RemixActionRegenerateImages {
    /** Describe the visual style to apply to every image (e.g. "watercolor painting", "cinematic film still"). */
    stylePrompt: string;
    /** Image generation quality tier. Defaults to `STANDARD`. */
    modelMode?: RemixActionRegenerateImages.ModelMode | undefined;
}

export namespace RemixActionRegenerateImages {
    /** Image generation quality tier. Defaults to `STANDARD`. */
    export const ModelMode = {
        Standard: "STANDARD",
        High: "HIGH",
    } as const;
    export type ModelMode = (typeof ModelMode)[keyof typeof ModelMode];
}
