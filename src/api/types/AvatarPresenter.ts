// This file was auto-generated by Fern from our API Definition.

/**
 * A pre-built avatar presenter you can pass to `POST /v1/tools/generate-avatar` via `avatarPresenterId`.
 */
export interface AvatarPresenter {
    /** Opaque presenter id (e.g. `vg_pres_...`). Pass this value as `avatarPresenterId` to `POST /v1/tools/generate-avatar`. */
    avatarPresenterId: string;
    /** Visual gender presentation of the presenter. */
    displayableGender: AvatarPresenter.DisplayableGender;
    /** Still image of the presenter. */
    imageUrl: string;
    /** Smaller image of the presenter, suitable for thumbnails or list rows. */
    thumbnailUrl: string;
    /** Short preview clip of the presenter speaking. */
    previewVideoUrl: string;
}

export namespace AvatarPresenter {
    /** Visual gender presentation of the presenter. */
    export const DisplayableGender = {
        Male: "MALE",
        Female: "FEMALE",
        Neutral: "NEUTRAL",
    } as const;
    export type DisplayableGender = (typeof DisplayableGender)[keyof typeof DisplayableGender];
}
