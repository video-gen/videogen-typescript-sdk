// This file was auto-generated by Fern from our API Definition.

/**
 * A text-to-speech voice.
 */
export interface TtsVoice {
    /** Voice id (e.g. `vg_voic_...`). Pass as `voiceId` to `POST /v1/tools/text-to-speech`. */
    voiceId: string;
    /** TTS provider (e.g. `ELEVEN_LABS`, `GOOGLE`). */
    providerName: string;
    /** Locale tag for the voice (e.g. `en-US`, `es-ES`). */
    languageCode: string;
    /** Human-readable voice name. */
    displayName: string;
    /** Voice gender. */
    displayGender: TtsVoice.DisplayGender;
    /** Accent (e.g. `american`, `british`). */
    accent?: (string | null) | undefined;
    /** Description of the voice. */
    description?: (string | null) | undefined;
}

export namespace TtsVoice {
    /** Voice gender. */
    export const DisplayGender = {
        Male: "MALE",
        Female: "FEMALE",
        Neutral: "NEUTRAL",
    } as const;
    export type DisplayGender = (typeof DisplayGender)[keyof typeof DisplayGender];
}
