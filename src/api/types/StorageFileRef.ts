// This file was auto-generated by Fern from our API Definition.

/**
 * Reference to an existing workspace file by its opaque id and asset kind. Use this in tool requests instead of inlining the full `StorageFile` payload.
 */
export interface StorageFileRef {
    /** Opaque file id (e.g. `vg_file_...`) returned from a previous tool result or `GET /v1/files`. */
    storageFileId: string;
    /** Asset kind. Must match the file the id refers to. */
    type: StorageFileRef.Type;
}

export namespace StorageFileRef {
    /** Asset kind. Must match the file the id refers to. */
    export const Type = {
        Image: "IMAGE",
        Video: "VIDEO",
        Audio: "AUDIO",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
