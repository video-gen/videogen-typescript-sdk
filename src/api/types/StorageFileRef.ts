// This file was auto-generated by Fern from our API Definition.

/**
 * Reference to a file by id and kind. Use in tool request bodies instead of a full `StorageFile`.
 */
export interface StorageFileRef {
    /** File id from a tool result or `GET /v1/files`. */
    storageFileId: string;
    /** Asset kind. Must match the file the id refers to. */
    type: StorageFileRef.Type;
}

export namespace StorageFileRef {
    /** Asset kind. Must match the file the id refers to. */
    export const Type = {
        Image: "IMAGE",
        Video: "VIDEO",
        Audio: "AUDIO",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
