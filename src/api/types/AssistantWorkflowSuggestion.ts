// This file was auto-generated by Fern from our API Definition.

/**
 * A starting-point workflow the assistant suggests for the conversation. Act on it with `POST /v1/assistant/chats/{projectId}/actions/{actionId}` to select the workflow and continue.
 */
export interface AssistantWorkflowSuggestion {
    /** Opaque id to pass to the act-on-action endpoint to select this workflow. */
    actionId: string;
    /** Identifier of the suggested workflow (e.g. `SCRIPT_TO_VIDEO`, `SLIDESHOW_TO_VIDEO`). Treat as an opaque label; new workflow types may be added over time. */
    workflowType: string;
    /** Short human-readable name of the suggested workflow. */
    title: string;
    /** One-line explanation of what this workflow will do. */
    description: string;
}
