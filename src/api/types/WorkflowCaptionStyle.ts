// This file was auto-generated by Fern from our API Definition.

import type * as VideoGenApi from "../index.js";

/**
 * Caption styling. Any omitted field falls back to the VideoGen default caption style. Provide an empty object (`{}`) to keep the default style but ensure captions are shown. Pass `null` for the whole `captionStyle` field to hide captions entirely.
 */
export interface WorkflowCaptionStyle {
    /** Font family name. */
    fontName?: string | undefined;
    /** Font size in pixels at 1080p. */
    fontSize?: number | undefined;
    /** Numeric font weight (400 = regular, 700 = bold). */
    fontWeight?: number | undefined;
    textColor?: VideoGenApi.WorkflowRgbColor | undefined;
    textJustification?: WorkflowCaptionStyle.TextJustification | undefined;
    /** Vertical position of the caption block in the frame. */
    verticalAlignment?: WorkflowCaptionStyle.VerticalAlignment | undefined;
    /** Outline color around glyphs, or null for no outline. */
    strokeColor?: (VideoGenApi.WorkflowRgbColor | null) | undefined;
    /** Outline thickness in pixels. */
    strokeWeight?: number | undefined;
    /** Background drawn behind the text, or null for no background. */
    backgroundStyle?: (VideoGenApi.WorkflowCaptionBackgroundStyle | null) | undefined;
    /** Color applied to the currently spoken word for karaoke-style highlighting, or null to keep the base text color. */
    spokenTextColor?: (VideoGenApi.WorkflowRgbColor | null) | undefined;
    /** Outline color applied to the currently spoken word, or null. */
    spokenTextStrokeColor?: (VideoGenApi.WorkflowRgbColor | null) | undefined;
    /** When true, a word keeps the spoken-text color after it has been spoken instead of reverting. */
    persistSpokenTextColor?: boolean | undefined;
}

export namespace WorkflowCaptionStyle {
    export const TextJustification = {
        Left: "LEFT",
        Center: "CENTER",
        Right: "RIGHT",
    } as const;
    export type TextJustification = (typeof TextJustification)[keyof typeof TextJustification];
    /** Vertical position of the caption block in the frame. */
    export const VerticalAlignment = {
        Top: "TOP",
        Middle: "MIDDLE",
        Bottom: "BOTTOM",
    } as const;
    export type VerticalAlignment = (typeof VerticalAlignment)[keyof typeof VerticalAlignment];
}
