// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         projectId: "projectId",
 *         assistantActionId: "assistantActionId"
 *     }
 */
export interface ActOnAssistantActionRequest {
    /** The project id (e.g. `vg_proj_...`). */
    projectId: string;
    /** The `assistantActionId` of the action to act on, from a prior `AssistantTurnResponse`. */
    assistantActionId: string;
    /** Whether to approve (apply) or reject the action. Defaults to `APPROVE`. */
    decision?: ActOnAssistantActionRequest.Decision;
}

export namespace ActOnAssistantActionRequest {
    /** Whether to approve (apply) or reject the action. Defaults to `APPROVE`. */
    export const Decision = {
        Approve: "APPROVE",
        Reject: "REJECT",
    } as const;
    export type Decision = (typeof Decision)[keyof typeof Decision];
}
